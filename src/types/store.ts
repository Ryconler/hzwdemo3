export type StoreType = {
  storeCode: string
  storeName: string
}

export type StoreDetailType = {
  store_code: string
  store_name: string
  address_city: string
  city: number
  photo: string
  address_street: string
  distance: number
}
